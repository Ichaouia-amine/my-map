export interface Overlay {
  name: string;
  active: boolean;
  hasMapVisible: boolean;
  isMinimizable: boolean;
  isMinimized: boolean;
  mobileReady: boolean;
  iconClass: string;
  title: string;
}
