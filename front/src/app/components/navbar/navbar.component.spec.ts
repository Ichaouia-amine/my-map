/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';
import { AppModuleForTest } from './../../app.module.spec';
import { NavbarComponent } from './navbar.component';
/*describe('NavbarComponent', () => {
  let component: NavbarComponent;
  let fixture: ComponentFixture<NavbarComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [AppModuleForTest]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NavbarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should toggleCollapse', () => {
    // spyOn(NavbarComponent, 'toggleCollapse');
    component.isCollapsed = false;
    component.toggleCollapse();
    expect(component.isCollapsed).toBeTruthy();
    component.toggleCollapse();
    expect(component.isCollapsed).toBeFalsy();
  });

  it('should callLogout', () => {
    // spyOn(NavbarComponent, 'logout');
    component.isCollapsed = false;
    component.logout();
    // expect(component.toggleCollapse).toHaveBeenCalled();
    // expect(component.authenticationService.logout).toBeFalsy();
    expect(component.isCollapsed).toBeTruthy();
  });

  it('should callLogout', () => {
    component.isCollapsed = false;
    component.logout();
    expect(component.isCollapsed).toBeTruthy();
  });

  it('should callusers', () => {
    component.isCollapsed = false;
    // component.users();
    expect(component.isCollapsed).toBeFalsy();
  });

  it('should callSearch', () => {
    component.isCollapsed = false;
    component.callSearch();
    expect(component.isCollapsed).toBeTruthy();
  });

  it('should callBaseMapTools', () => {
    component.isCollapsed = false;
    component.callBaseMapTools();
    expect(component.isCollapsed).toBeTruthy();
  });

  it('should callTOC', () => {
    component.isCollapsed = false;
    component.callTOC();
    expect(component.isCollapsed).toBeTruthy();
  });

  it('should callPrint', () => {
    component.isCollapsed = false;
    component.callPrint();
    expect(component.isCollapsed).toBeTruthy();
  });

  it('should callAttributesTable', () => {
    component.isCollapsed = false;
    component.callAttributesTable();
    expect(component.isCollapsed).toBeTruthy();
  });

});*/
